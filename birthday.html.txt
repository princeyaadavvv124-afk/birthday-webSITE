<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Happy Birthday Simran!</title>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@700&family=Poppins:wght@300;500;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --neon-pink: #ff00cc;
            --neon-purple: #3333ff;
            --bg-color: #050505;
            --glass-bg: rgba(255, 255, 255, 0.05);
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            background-color: var(--bg-color);
            color: white;
            font-family: 'Poppins', sans-serif;
            overflow-x: hidden;
            min-height: 100vh;
        }

        #star-canvas {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            background: radial-gradient(ellipse at bottom, #1b2735 0%, #090a0f 100%);
        }

        #password-screen {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.95);
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            z-index: 1000;
            transition: opacity 1s ease;
        }

        .password-box {
            background: var(--glass-bg);
            padding: 40px;
            border-radius: 20px;
            border: 1px solid rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            text-align: center;
            box-shadow: 0 0 30px rgba(255, 0, 204, 0.2);
        }

        .password-box h2 {
            margin-bottom: 20px;
            font-family: 'Playfair Display', serif;
            color: var(--neon-pink);
            text-shadow: 0 0 10px var(--neon-pink);
        }

        input[type="password"] {
            padding: 15px;
            border-radius: 30px;
            border: 2px solid var(--neon-purple);
            background: transparent;
            color: white;
            text-align: center;
            font-size: 1.2rem;
            outline: none;
            margin-bottom: 20px;
            width: 250px;
            transition: 0.3s;
        }

        input[type="password"]:focus {
            box-shadow: 0 0 15px var(--neon-purple);
        }

        .enter-btn {
            padding: 12px 30px;
            border-radius: 30px;
            border: none;
            background: linear-gradient(45deg, var(--neon-pink), var(--neon-purple));
            color: white;
            font-weight: bold;
            cursor: pointer;
            transition: 0.3s;
            box-shadow: 0 0 15px var(--neon-pink);
        }

        .enter-btn:hover {
            transform: scale(1.05);
            box-shadow: 0 0 25px var(--neon-pink);
        }

        #main-content {
            display: none;
            opacity: 0;
            transition: opacity 2s ease;
            padding: 20px;
            text-align: center;
        }

        .hero {
            height: 80vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            position: relative;
        }

        h1.title {
            font-family: 'Playfair Display', serif;
            font-size: 3.5rem;
            margin-bottom: 20px;
            background: linear-gradient(to right, #fff, var(--neon-pink));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            animation: glow 2s infinite alternate;
        }

        @keyframes glow {
            from { text-shadow: 0 0 10px rgba(255, 0, 204, 0.5); }
            to { text-shadow: 0 0 20px rgba(51, 51, 255, 0.8), 0 0 10px white; }
        }

        .countdown-container {
            display: flex;
            gap: 20px;
            margin-top: 30px;
        }

        .time-box {
            background: var(--glass-bg);
            padding: 20px;
            border-radius: 15px;
            min-width: 100px;
            border: 1px solid rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(5px);
        }

        .time-box span {
            display: block;
            font-size: 2rem;
            font-weight: bold;
            color: var(--neon-pink);
        }

        .time-box label {
            font-size: 0.8rem;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        .photo-frame {
            width: 200px;
            height: 200px;
            border-radius: 50%;
            border: 4px solid var(--neon-purple);
            box-shadow: 0 0 30px var(--neon-purple), inset 0 0 20px var(--neon-pink);
            overflow: hidden;
            margin: 40px auto;
            position: relative;
            animation: float 6s ease-in-out infinite;
        }

        .photo-frame img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-20px); }
        }

        .surprise-section {
            padding: 50px 20px;
            min-height: 50vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }

        .surprise-btn {
            padding: 15px 40px;
            font-size: 1.2rem;
            border-radius: 50px;
            border: 2px solid var(--neon-pink);
            background: transparent;
            color: white;
            cursor: pointer;
            position: relative;
            overflow: hidden;
            transition: 0.4s;
            box-shadow: 0 0 10px var(--neon-pink);
            margin-bottom: 30px;
        }

        .surprise-btn:hover {
            background: var(--neon-pink);
            box-shadow: 0 0 30px var(--neon-pink);
        }

        .hidden-message {
            display: none;
            background: var(--glass-bg);
            padding: 40px;
            border-radius: 20px;
            max-width: 600px;
            width: 90%;
            border: 1px solid var(--neon-pink);
            box-shadow: 0 0 20px rgba(255, 0, 204, 0.3);
            animation: fadeIn 1s ease;
        }

        .hidden-message h2 {
            color: var(--neon-pink);
            margin-bottom: 15px;
            font-size: 2rem;
        }

        .hidden-message p {
            font-size: 1.1rem;
            line-height: 1.6;
            margin-bottom: 20px;
        }

        .slideshow-container {
            width: 100%;
            height: 300px;
            border-radius: 15px;
            overflow: hidden;
            margin-top: 20px;
            border: 2px solid var(--neon-purple);
        }

        .slide {
            width: 100%;
            height: 100%;
            object-fit: cover;
            display: none;
        }

        .slide.active {
            display: block;
            animation: zoomIn 0.5s ease;
        }

        @keyframes zoomIn {
            from { transform: scale(1.1); opacity: 0; }
            to { transform: scale(1); opacity: 1; }
        }

        .heart {
            position: absolute;
            color: var(--neon-pink);
            font-size: 20px;
            animation: floatUp 4s linear infinite;
            opacity: 0;
            z-index: 0;
        }

        @keyframes floatUp {
            0% { transform: translateY(100vh) scale(0.5); opacity: 0; }
            50% { opacity: 0.8; }
            100% { transform: translateY(-10vh) scale(1.2); opacity: 0; }
        }

        #fireworks {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 999;
        }

        @media (max-width: 600px) {
            h1.title { font-size: 2.2rem; }
            .countdown-container { gap: 10px; }
            .time-box { min-width: 70px; padding: 10px; }
            .time-box span { font-size: 1.5rem; }
        }
    </style>
</head>
<body>
    <audio id="bg-music" loop>
        <source src="https://www.soundhelix.com/examples/mp3/SoundHelix-Song-1.mp3" type="audio/mpeg">
    </audio>
    <canvas id="star-canvas"></canvas>
    <canvas id="fireworks"></canvas>

    <div id="password-screen">
        <div class="password-box">
            <h2>🔒 Secure Access</h2>
            <p style="margin-bottom: 15px; color: #ccc;">Enter the secret code to enter</p>
            <input type="password" id="password-input" placeholder="Enter Password">
            <br>
            <button class="enter-btn" onclick="checkPassword()">Unlock</button>
            <p id="error-msg" style="color: red; margin-top: 10px; display: none;">Incorrect Password!</p>
        </div>
    </div>

    <div id="main-content">
        <section class="hero">
            <h1 class="title">Simran's Birthday is Coming Soon 🎉</h1>
            <div class="photo-frame">
                <img src="https://images.unsplash.com/photo-1494790108377-be9c29b29330?ixlib=rb-1.2.1&auto=format&fit=crop&w=500&q=80" alt="Simran">
            </div>
            <div class="countdown-container">
                <div class="time-box"><span id="days">00</span><label>Days</label></div>
                <div class="time-box"><span id="hours">00</span><label>Hours</label></div>
                <div class="time-box"><span id="minutes">00</span><label>Mins</label></div>
                <div class="time-box"><span id="seconds">00</span><label>Secs</label></div>
            </div>
        </section>

        <section class="surprise-section">
            <button class="surprise-btn" onclick="openSurprise()">Open Birthday Surprise 🎁</button>
            <div class="hidden-message" id="surprise-msg">
                <h2>Happy Birthday Simran! 💖</h2>
                <p>You are my best friend and very special to me. May this year bring you as much joy as you bring to everyone around you.</p>
                <div class="slideshow-container">
                    <img src="https://images.unsplash.com/photo-1517841905240-472988babdf9?ixlib=rb-1.2.1&auto=format&fit=crop&w=500&q=80" class="slide active" alt="Memory 1">
                    <img src="https://images.unsplash.com/photo-1529626455594-4ff0802cfb7e?ixlib=rb-1.2.1&auto=format&fit=crop&w=500&q=80" class="slide" alt="Memory 2">
                    <img src="https://images.unsplash.com/photo-1488426862026-3ee34a7d66df?ixlib=rb-1.2.1&auto=format&fit=crop&w=500&q=80" class="slide" alt="Memory 3">
                </div>
            </div>
        </section>
    </div>

    <script>
        // Password Check
        function checkPassword() {
            const input = document.getElementById('password-input').value;
            const errorMsg = document.getElementById('error-msg');
            const passwordScreen = document.getElementById('password-screen');
            const mainContent = document.getElementById('main-content');
            const music = document.getElementById('bg-music');

            if (input === "simran20") {
                passwordScreen.style.opacity = '0';
                setTimeout(() => {
                    passwordScreen.style.display = 'none';
                    mainContent.style.display = 'block';
                    setTimeout(() => { mainContent.style.opacity = '1'; }, 100);
                }, 1000);
                music.volume = 0.3;
                music.play().catch(e => console.log("Audio play failed:", e));
            } else {
                errorMsg.style.display = 'block';
                document.getElementById('password-input').value = '';
            }
        }

        // Allow Enter key to submit password
        document.getElementById('password-input').addEventListener('keypress', function(e) {
            if (e.key === 'Enter') {
                checkPassword();
            }
        });

        // Countdown Timer
        function updateCountdown() {
            const now = new Date();
            let year = now.getFullYear();
            let birthday = new Date(year, 1, 20);

            if (now > birthday) {
                birthday = new Date(year + 1, 1, 20);
            }

            const diff = birthday - now;
            const days = Math.floor(diff / (1000 * 60 * 60 * 24));
            const hours = Math.floor((diff % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
            const minutes = Math.floor((diff % (1000 * 60 * 60)) / (1000 * 60));
            const seconds = Math.floor((diff % (1000 * 60)) / 1000);

            document.getElementById('days').textContent = days.toString().padStart(2, '0');
            document.getElementById('hours').textContent = hours.toString().padStart(2, '0');